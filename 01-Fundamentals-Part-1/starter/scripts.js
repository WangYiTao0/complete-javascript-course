// let js = "amazing";
// if (js === "amazing") alert("JavaScript is FUN! 😍");
// console.log(40 + 8 + 23 - 110);

const firstName = "Jonas";

const jonos = `I'm ${firstName}, a teacher and a student!`;
console.log(jonos); 

console.log(`String
    with
    multiple lines`);

// falsy values: 0, '', undefined, null, NaN
// truthy values: 'Jonas', 23, {}, [], true, false

// === strict equality operator dont convert types
// == loose equality operator convert types

prompt("What is your name?")

// statements
// expressions: a piece of code that produces a value

// 3 + 4; // expression produces a value